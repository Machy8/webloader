;(function ($) {
	var codeFragment = 'jQuery! muhehe';

	console.log(codeFragment);
})(jQuery);

var
	beersCount,
	endeSchluss = 10,
	morning = 1000;

for (beersCount = 0; beersCount < endeSchluss; beersCount++) {
	console.log('Beer number ' + beersCount);
}

alert('Goingdmddqwd hommme!');

setTimeout(function () {
	alert('F** hangover!')
}, morning);
